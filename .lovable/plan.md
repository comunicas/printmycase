

# Card com fundo branco na imagem

## Correção
No `ProductCard.tsx`, trocar `bg-muted` por `bg-white` no container da imagem (linha 20).

| Arquivo | Mudança |
|---------|---------|
| `src/components/ProductCard.tsx` | `bg-muted` → `bg-white` no div da imagem |

